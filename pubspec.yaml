name: spotly
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_riverpod: ^2.5.3
  uuid: ^4.5.1
  url_launcher: ^6.0.9
  image_picker: ^1.1.2
  path_provider: ^2.1.4
  shared_preferences: ^2.0.11
  intl: ^0.17.0
  location: ^7.0.1
  geolocator: ^9.0.2 # Lägg geolocator här i samma dependencies
  http: ^1.2.2
  flutter_swipe_action_cell: ^3.1.5

  # Database
  sqflite: ^2.4.0
  path: ^1.9.0

  # Share & Import
  share_plus: ^10.1.0
  device_info_plus: ^11.1.0
  file_picker: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
